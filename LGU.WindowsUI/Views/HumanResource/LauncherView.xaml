<UserControl x:Class="LGU.Views.HumanResource.LauncherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"    
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <ResourceDictionary Source="../../Styles/Default.xaml"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <materialDesign:Card Grid.Column="0" Style="{DynamicResource ContainerCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <materialDesign:ColorZone Grid.Row="0" Style="{DynamicResource HeaderColorZone}">
                    <TextBlock Text="HRDO" Style="{DynamicResource HeaderTextBlock}"/>
                </materialDesign:ColorZone>
                <ItemsControl Grid.Row="1" Style="{DynamicResource NavigationItemsControl}">
                    <ItemsControl.Resources>
                        <Style TargetType="{x:Type materialDesign:TransitioningContent}" BasedOn="{StaticResource NavigationTransitioningContent}"/>
                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource NavigationButton}"/>
                    </ItemsControl.Resources>
                    <materialDesign:TransitioningContent>
                        <Button Content="Departments" Command="{Binding NavigateCommand}" CommandParameter="DepartmentManagementView"/>
                    </materialDesign:TransitioningContent>
                    <materialDesign:TransitioningContent OpeningEffectsOffset="{materialDesign:IndexedItemOffsetMultiplier 0:0:0.5}">
                        <Button Content="Employees" Command="{Binding NavigateCommand}" CommandParameter="EmployeeManagementView"/>
                    </materialDesign:TransitioningContent>
                </ItemsControl>
            </Grid>
        </materialDesign:Card>
        <TextBlock Grid.Column="1" Text="Human Resouce &amp; Development Office" Style="{DynamicResource ExtraLargeTextBlock}"/>
    </Grid>
</UserControl>
