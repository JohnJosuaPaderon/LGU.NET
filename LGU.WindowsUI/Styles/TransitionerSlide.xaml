<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Style x:Key="StackTransitionerSlide" TargetType="{x:Type materialDesign:TransitionerSlide}">
        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
        <Setter Property="BackwardWipe">
            <Setter.Value>
                <materialDesign:SlideWipe Direction="Down"/>
            </Setter.Value>
        </Setter>
        <Setter Property="ForwardWipe">
            <Setter.Value>
                <materialDesign:SlideWipe Direction="Up"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="HorizontalStackTransitionerSlide" TargetType="{x:Type materialDesign:TransitionerSlide}">
        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
        <Setter Property="BackwardWipe">
            <Setter.Value>
                <materialDesign:SlideWipe Direction="Right"/>
            </Setter.Value>
        </Setter>
        <Setter Property="ForwardWipe">
            <Setter.Value>
                <materialDesign:SlideWipe Direction="Left"/>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>