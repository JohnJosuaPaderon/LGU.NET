<UserControl x:Class="LGU.HumanResource.Views.TimeLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <ResourceDictionary Source="../../Styles/Core.xaml"/>
    </UserControl.Resources>
    <Grid Margin="{DynamicResource FormMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <materialDesign:Card Grid.Row="0">
            <TextBlock Text="City Government of Navotas" Style="{DynamicResource FormHeaderTextBlock}"/>
        </materialDesign:Card>
        <Grid Grid.Row="1">
            
        </Grid>
        <materialDesign:Card Grid.Row="2">
            
        </materialDesign:Card>
    </Grid>
</UserControl>
