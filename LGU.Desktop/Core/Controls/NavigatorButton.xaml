<UserControl x:Class="LGU.Core.Controls.NavigatorButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Margin="4"
             x:Name="this">
    <Grid DataContext="{Binding ElementName=this}">
        <Button Style="{DynamicResource MaterialDesignFlatButton}" Height="256" Width="256" Command="{Binding NavigateCommand}" CommandParameter="{Binding NavigateCommandParameter}">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <materialDesign:PackIcon Grid.Row="0" Kind="{Binding Icon,FallbackValue=Close}" HorizontalAlignment="Center" Width="128" Height="128"/>
                <TextBlock Grid.Row="1" Text="{Binding Caption,FallbackValue=Caption Text}" FontSize="32" HorizontalAlignment="Center"/>
            </Grid>
        </Button>
    </Grid>
</UserControl>
